# Specification documents

Forthcoming. See the top-level [`spec/README.md`](../README.md) → **Repository layout** for the planned files and their scopes.

Each file here will be one normative concern: VOI, Entity, Capability, tiers, temporal, REST, MCP, A2A, OpenClaw, discovery, `skills.md`, `agent.md`, JSON-LD.

The conceptual rationale that motivates these shapes — what each tier means, why VOIs are immutable, why bi-temporality, what the system explicitly does not verify — is published at [docs.bukti.ai](https://docs.bukti.ai). The normative wire-level specification lives here.
