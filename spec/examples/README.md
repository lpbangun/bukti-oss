# Examples

Minimal compliant payloads for every object and interop surface in the protocol.

Each example is a complete, valid payload — no ellipses, no placeholders — so an implementer can validate their own output against something concrete.

Forthcoming in v0.1-draft:
- `voi.json` — one VOI of each evidence type.
- `entity-person.json`, `entity-agent.json`.
- `profile-person.json`, `profile-agent.json`.
- `capability-provenance.json`.
- `agent-card.json`.
- `skills-md.md`, `agent-md.md`.
- `json-ld-person.json`, `json-ld-agent.json`.
- `llms-txt.txt`.
- MCP tool request/response pairs (one per tool).
