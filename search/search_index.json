{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Bukti","text":"<p>The <code>bukti</code> Python package is a thin client for the Bukti capability intelligence platform. It wraps the public HTTP API at <code>api.bukti.ai</code> and implements the open Bukti Protocol specification that lives in <code>spec/</code>.</p> <p>Use these docs to:</p> <ul> <li>Install and make your first call.</li> <li>Browse the client and CLI surface.</li> <li>See how the layers fit together.</li> <li>Understand what v0.1 does not do.</li> <li>Jump into the protocol specification.</li> </ul>"},{"location":"#who-this-is-for","title":"Who this is for","text":"<ul> <li>Python developers who want to query Bukti profiles, capabilities, and   evidence chains from a script, a notebook, or a backend service.</li> <li>Agent developers who want an evidence-grounded signal about other   agents (cost, latency, modality, capability provenance).</li> <li>Protocol implementers who want to build a compatible client in another   language, or a compatible server \u2014 both of which the specification covers.</li> </ul>"},{"location":"#minimum-example","title":"Minimum example","text":"<pre><code>from bukti import BuktiClient\n\nwith BuktiClient() as bukti:\n    profile = bukti.get_profile(\"agent_0be6d7cce0e8\")\n    for cap in profile.capabilities:\n        print(f\"{cap.tier:&lt;12} {cap.name}  ({cap.evidence_score:.2f})\")\n</code></pre> <p>That is the whole library in miniature. Everything else is variations on this: list every capability, fetch the underlying VOIs for one capability, or scan the provenance manifest for a whole profile.</p>"},{"location":"architecture/","title":"Architecture","text":"<pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510     \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510     HTTPS      \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502  bukti   \u2502 \u2500\u2500\u25b6 \u2502 BuktiClient  \u2502 \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25b6 \u2502 api.bukti.ai  \u2502\n\u2502  (CLI)   \u2502     \u2502  (Pydantic)  \u2502                \u2502  (closed)     \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518     \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518                \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n      \u25b2                                                  \u2502\n      \u2502                                                  \u2502 implements\n      \u2514\u2500\u2500\u2500\u2500\u2500 implements \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25b6\u2502\n                                                          \u2502\n                                            \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                                            \u2502   Bukti Protocol     \u2502\n                                            \u2502       spec/          \u2502\n                                            \u2502  (open, Apache-2.0)  \u2502\n                                            \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre>"},{"location":"architecture/#layers","title":"Layers","text":"<p>CLI (<code>src/bukti/cli.py</code>). Click + Rich. Parses flags, picks an output mode (rich or JSON), constructs a <code>BuktiClient</code>, hands results to a formatter, maps <code>BuktiError</code> to a non-zero exit code.</p> <p>Client (<code>src/bukti/client.py</code>). Synchronous HTTP wrapper on <code>httpx</code>. Every method takes primitive arguments and returns a Pydantic model. One retry on transient 5xx; everything else surfaces as a typed exception.</p> <p>Models (<code>src/bukti/models.py</code>). Pydantic v2 mirrors of the public API response shapes. Extra fields from the server are ignored, so additive API changes do not break pinned clients.</p> <p>Server (<code>api.bukti.ai</code>). Closed-source. Runs the pipeline that turns public artifacts into VOIs (Verified Outcome Instances) and serves the public read surface. This repository has no backend code.</p> <p>Specification (<code>spec/</code>). Implementation-agnostic description of the wire formats the client depends on. Anyone can implement it.</p>"},{"location":"architecture/#request-path","title":"Request path","text":"<ol> <li>Caller invokes <code>bukti.get_profile(\"agent_0be6d7cce0e8\")</code> (or the    equivalent CLI command).</li> <li>Client assembles <code>GET /v1/profile/agent_0be6d7cce0e8</code> against the    configured base URL.</li> <li>Server returns JSON; <code>Profile.model_validate(...)</code> parses it.</li> <li>Client returns the typed object; CLI formats it.</li> </ol>"},{"location":"architecture/#what-the-server-does-that-the-client-does-not","title":"What the server does that the client does not","text":"<p>Only the hosted Bukti backend runs the ingestion pipeline, the evidence scoring engine, the ontology mapper, the entity resolver, and the profile-generation LLMs. The client only consumes the published output. The protocol specification describes the output format; it does not describe the backend's internal implementation.</p>"},{"location":"limitations/","title":"Limitations","text":"<p><code>bukti</code> v0.1 is deliberately minimal. This page is an honest list of what it does not yet do, so evaluators and integrators can plan around it.</p>"},{"location":"limitations/#shape-stability","title":"Shape stability","text":"<ul> <li>The client's public surface is a working draft. Methods may be renamed   before 1.0.</li> <li>The wire formats in <code>spec/</code> are the longer-term contract;   breaking changes there follow the spec's own versioning and changelog.</li> </ul>"},{"location":"limitations/#scope-v01-is-read-only","title":"Scope (v0.1 is read-only)","text":"<ul> <li>No agent registration. <code>POST /v1/agent/register</code> exists on the server but   is not wrapped.</li> <li>No profile editing. Manual edits, section edits, and redesigns are   server-only.</li> <li>No search. <code>POST /v1/search</code> is wrappable but deferred to v0.2 because it   needs a typed decomposed-query model.</li> <li>No MCP client. <code>api.bukti.ai/mcp</code> is available for MCP-capable agents; a   Python MCP wrapper lands in v0.2.</li> </ul>"},{"location":"limitations/#runtime","title":"Runtime","text":"<ul> <li>Synchronous only. No <code>async</code> client today.</li> <li>One retry on transient 5xx. No retry on 429, no exponential backoff, no   circuit breaker. If you need those, wrap <code>BuktiClient</code> in your own retry   loop using <code>tenacity</code> or equivalent.</li> <li>Rate limits are enforced by <code>api.bukti.ai</code> and surface as   <code>BuktiRateLimitError</code>. The client does not currently read the   <code>Retry-After</code> header.</li> <li>No response caching. Every call hits the network.</li> <li>No logging unless the caller configures <code>httpx</code> logging explicitly. The   library does not emit its own log lines.</li> </ul>"},{"location":"limitations/#platforms","title":"Platforms","text":"<ul> <li>Tested on Python 3.10, 3.11, and 3.12.</li> <li>CI runs on Ubuntu. macOS is expected to work but is not in the matrix.   Windows is untested.</li> </ul>"},{"location":"limitations/#evidence-layer-caveats-inherited-from-bukti","title":"Evidence-layer caveats (inherited from Bukti)","text":"<ul> <li>Evidence scores reflect evidence density, not competence. The naming   (<code>evidence_score</code>, tier labels) is deliberate.</li> <li><code>self-declared</code> tier means the system saw one signal from a single source;   it does not mean the claim is false.</li> <li><code>growth_*</code> capability IDs live outside the curated seed ontology. They are   real signals, but their cross-entity comparability is weaker than nodes in   the validated taxonomy.</li> </ul>"},{"location":"limitations/#what-does-not-ship-here-at-all","title":"What does not ship here at all","text":"<ul> <li>The closed-source Bukti backend (pipeline, orchestrator, model routing,   scoring engine, entity resolver, LLM prompts, graph writes).</li> <li>Proprietary weight values or thresholds beyond what the hosted product   already publishes.</li> <li>Private data of any kind.</li> </ul>"},{"location":"setup/","title":"Setup","text":""},{"location":"setup/#install","title":"Install","text":"<p>Python 3.10 or newer is required.</p> <pre><code>pip install git+https://github.com/bukti-ai/bukti\n</code></pre> <p>To install for development, including the test suite and docs tooling:</p> <pre><code>git clone https://github.com/bukti-ai/bukti\ncd bukti\npip install -e \".[dev]\"\n</code></pre>"},{"location":"setup/#environment-variables","title":"Environment variables","text":"<p>The client has no mandatory configuration. Optional variables:</p> Variable Purpose <code>BUKTI_API_KEY</code> API key forwarded as <code>X-API-Key</code>. Most read-only endpoints do not require a key today. <p>The CLI picks <code>BUKTI_API_KEY</code> up automatically; programmatic callers pass <code>api_key=</code> to <code>BuktiClient(...)</code>.</p>"},{"location":"setup/#first-call","title":"First call","text":"<pre><code>from bukti import BuktiClient\n\nwith BuktiClient() as bukti:\n    profile = bukti.get_profile(\"agent_0be6d7cce0e8\")\n    print(profile.display_name)\n</code></pre> <p>Or from the shell:</p> <pre><code>bukti profile agent_0be6d7cce0e8\n</code></pre> <p>If the entity is not published or does not exist the call raises <code>BuktiNotFoundError</code> (or the CLI prints an <code>error:</code> line and exits with code 1).</p>"},{"location":"setup/#common-errors","title":"Common errors","text":"Symptom Likely cause What to do <code>BuktiNotFoundError: 404 Profile not found</code> Entity ID is stale or draft. List published profiles at <code>https://api.bukti.ai/v1/profiles</code>. <code>BuktiAuthError</code> Key is wrong or the endpoint requires one. Confirm the value of <code>BUKTI_API_KEY</code>. <code>BuktiRateLimitError</code> Too many requests from this IP/key. Back off; the client does not retry 429 automatically. <code>BuktiServerError</code> Transient upstream 5xx after one retry. Retry later; if persistent, file an issue. Network-level <code>BuktiError</code> DNS / connectivity. Check <code>api.bukti.ai</code> is reachable from your environment."},{"location":"spec/","title":"Specification","text":"<p>The Bukti Protocol specification lives in <code>spec/</code> at the root of the repository. This page is a pointer and a summary; the specification itself is the normative source.</p>"},{"location":"spec/#what-the-spec-covers","title":"What the spec covers","text":"<ul> <li><code>spec/README.md</code> \u2014 top-level overview, design principles, cross-reference from spec sections to live <code>bukti.ai</code> surfaces, and the in-scope / out-of-scope lists.</li> <li><code>spec/docs/</code> \u2014 normative specification files (one concern per file: VOI, Entity, Capability, tiers, temporal semantics, REST, MCP, A2A, OpenClaw, discovery files, <code>skills.md</code>, <code>agent.md</code>, JSON-LD).</li> <li><code>spec/schemas/</code> \u2014 machine-readable JSON Schema definitions (<code>$schema</code> = draft 2020-12) for every object in the protocol.</li> <li><code>spec/examples/</code> \u2014 minimal compliant payloads for every object and every interop surface.</li> <li><code>spec/CONTRIBUTING.md</code> \u2014 governance, style guide, and the list of change types that require discussion before a PR.</li> <li><code>spec/CHANGELOG.md</code> \u2014 independent version history for the specification.</li> </ul>"},{"location":"spec/#relationship-to-this-client","title":"Relationship to this client","text":"<p>The client in <code>src/bukti/</code> implements the read-only subset of the spec's REST surface. The Pydantic models in <code>src/bukti/models.py</code> mirror the shapes defined normatively in <code>spec/schemas/</code>. When the two disagree, <code>spec/</code> is the source of truth; file an issue.</p>"},{"location":"spec/#versioning","title":"Versioning","text":"<p>The client and the specification version independently:</p> Artifact Version today Versioning rule <code>bukti</code> Python client 0.1.0 Semver; breaking changes require a major bump once at 1.0. Bukti Protocol 0.1.0-draft Semver; draft suffix while shapes are still being finalized."},{"location":"spec/#how-to-read-the-spec-for-an-implementation","title":"How to read the spec for an implementation","text":"<ol> <li>Start with <code>spec/README.md</code> for the mental model.</li> <li>Read the VOI, Entity, Capability, and tiers documents (in that order) for    the core objects.</li> <li>Read whichever interop surface documents you need (REST, MCP, A2A,    OpenClaw, discovery).</li> <li>Validate any payloads you produce against <code>spec/schemas/*.schema.json</code>.</li> <li>Compare against <code>spec/examples/*.json</code> for complete reference payloads.</li> </ol>"},{"location":"usage/","title":"Usage","text":""},{"location":"usage/#the-client","title":"The client","text":"<p><code>BuktiClient</code> is a synchronous context manager. Instantiate once per task, reuse the object, close when done (or use <code>with</code>).</p> <pre><code>from bukti import BuktiClient\n\nwith BuktiClient(base_url=\"https://api.bukti.ai\", api_key=None, timeout=30) as bukti:\n    ...\n</code></pre>"},{"location":"usage/#get_profileentity_id-profile","title":"<code>get_profile(entity_id) -&gt; Profile</code>","text":"<p>Returns a full published profile. Draft profiles return 404 to everyone except their owner.</p> <pre><code>profile = bukti.get_profile(\"agent_0be6d7cce0e8\")\nprofile.display_name\nprofile.entity_type       # \"person\" | \"ai_agent\" | \"organization\"\nprofile.domain            # e.g. \"software_engineering\"\nprofile.capabilities      # list[Capability]\nprofile.profile_url       # canonical public URL\n</code></pre>"},{"location":"usage/#list_capabilitiesentity_id-listcapability","title":"<code>list_capabilities(entity_id) -&gt; list[Capability]</code>","text":"<p>Every capability on the profile with its current tier, evidence score, and platform fan-out. Useful when you only need the capability layer and not the presentation content.</p> <pre><code>for cap in bukti.list_capabilities(\"agent_0be6d7cce0e8\"):\n    print(cap.tier, cap.name, cap.evidence_count)\n</code></pre>"},{"location":"usage/#get_provenanceentity_id-provenance","title":"<code>get_provenance(entity_id) -&gt; Provenance</code>","text":"<p>Aggregate manifest across the whole profile: VOI count, VOI IDs, and the distinct source platforms that contributed. Think of it as a receipts page for the profile as a whole.</p>"},{"location":"usage/#get_capability_provenanceentity_id-capability_id-capabilityprovenance","title":"<code>get_capability_provenance(entity_id, capability_id) -&gt; CapabilityProvenance</code>","text":"<p>The full chain for one capability: every VOI, its evidence type, its platform, a snippet, and extraction confidence.</p> <pre><code>chain = bukti.get_capability_provenance(\n    \"agent_0be6d7cce0e8\",\n    \"growth_agentic_infra_debugging\",\n)\nfor ev in chain.evidence:\n    print(ev.evidence_type, ev.source_platform, ev.voi_id)\n</code></pre>"},{"location":"usage/#the-cli","title":"The CLI","text":"Command Output <code>bukti profile &lt;entity_id&gt;</code> Rich-formatted profile panel + capability count. <code>bukti capabilities &lt;entity_id&gt;</code> Table sorted by tier (verified \u2192 attested \u2192 self-declared). <code>bukti provenance &lt;entity_id&gt; &lt;capability_id&gt;</code> Header panel + one row per VOI. <code>bukti version</code> Client version. <p>Global flags:</p> <ul> <li><code>--json</code> \u2014 emit raw JSON instead of formatted output. Pipe to <code>jq</code> for   post-processing.</li> <li><code>--api-key</code> \u2014 explicit key, otherwise <code>BUKTI_API_KEY</code>.</li> <li><code>--base-url</code> \u2014 override (e.g. for a staging deployment).</li> </ul> <p>Example JSON output:</p> <pre><code>bukti --json capabilities agent_0be6d7cce0e8 | jq '.[].tier'\n</code></pre>"},{"location":"usage/#error-handling","title":"Error handling","text":"<pre><code>from bukti import BuktiClient, BuktiError, BuktiNotFoundError\n\nwith BuktiClient() as bukti:\n    try:\n        profile = bukti.get_profile(\"maybe_missing\")\n    except BuktiNotFoundError:\n        profile = None\n    except BuktiError as exc:\n        # Any other Bukti-level failure.\n        raise\n</code></pre> <p>All exceptions inherit from <code>BuktiError</code> and carry <code>status_code</code> when the failure came from an HTTP response.</p>"}]}